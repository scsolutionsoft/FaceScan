# Windows note:
# Use: py -m pip install --no-deps -r FaceScan.Web/FaceEngine/requirements.txt
# (face_recognition depends on package name "dlib", but on Windows we install prebuilt "dlib-bin")
opencv-python-headless>=4.10,<5.0
dlib-bin==19.24.6
face_recognition==1.3.0
face-recognition-models==0.3.0
click>=8.1,<9.0
Pillow>=11.0,<12.0
numpy>=1.26,<3.0
# Optional extra engines (install separately when the Python runtime supports them):
# onnxruntime>=1.19
# insightface>=0.7.3
# deepface>=0.0.93
